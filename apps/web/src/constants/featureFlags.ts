/**
 * Feature flags for gradual rollout of new features.
 * Set to `true` to enable a feature across the app.
 */
export const FEATURES = {
  /** Show the "Urgent" toggle on task creation / detail pages */
  URGENT: false,
} as const;
